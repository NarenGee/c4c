"use client"

export function AssignmentHint() {
  return (
    <p className="text-sm text-slate-600">
      <strong>Tap</strong> a task to select it, then <strong>tap</strong> a category to place it.
      You can also <strong>drag and drop</strong> — the page will auto-scroll while you drag.
    </p>
  )
}
